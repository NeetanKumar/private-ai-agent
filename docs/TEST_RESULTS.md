# Test Results: Private AI Agent Template

Generated by `make test` on 2026-09-21 07:11 UTC, at commit ebcbcec plus uncommitted changes, with Python 3.9.6 on Darwin.
Do not edit by hand. Run `make test` to regenerate it.

**Automated suite: 243 passed, 0 failed, 1 skipped, 244 total.**

## How to read this

Every automated check here ran against **stand-in servers**: small local programs that play the local
model and the frontier API over real network sockets. That makes the privacy and permission logic
testable, including connection counts, but it says nothing about how a real model behaves. The checks
that need a real GPU host are listed after the table and are **not run**. They are not counted as passing.

## Automated checks

| Check | Method | Result | Pass/Fail |
|---|---|---|---|
| **Phase 1: gateway** | | | |
| Gateway proxies a completion and reports the lane | Stand-in model server over real sockets | 3/3 tests passed | PASS |
| Health endpoint reports model-server state | Stand-in server up and down | 3/3 tests passed | PASS |
| Bearer token required, one session per user | Requests with no token and a wrong token | 2/2 tests passed | PASS |
| **Phase 2: taint engine, lanes, audit log** | | | |
| Default ships `auto_route_clean: false`; both values are tested | Read the shipped config; both settings run in the routing tests | 1/1 tests passed | PASS |
| Unregistered source is PRIVATE; CLEAN needs an explicit entry; context taint is the maximum; taint is sticky | Unit tests on the registry, fragments and session | 8/8 tests passed | PASS |
| Canary strings in PRIVATE sources never appear in frontier requests across 200 mixed queries (run with both settings) | 200 seeded queries mixing clean, private and public sources; the stand-in frontier server records every body | 2/2 tests passed | PASS |
| Tainted session + explicit consent + `auto_route_clean=true` is still blocked | Consent given, auto routing on, private context; explicit frontier request gets 403 | 2/2 tests passed | PASS |
| Turn N tainted, turn N+3 innocuous follow-up still blocked | Four turns with consent and auto routing on | 2/2 tests passed | PASS |
| `auto_route_clean=false` + CLEAN context: no egress until consent | Session scope and request scope; no connection before consent | 4/4 tests passed | PASS |
| `auto_route_clean=true` + CLEAN context: egress occurs and an audit record is written | Audit record fields and values checked | 1/1 tests passed | PASS |
| PRIVATE context: local only, no offer, no egress (both settings) | Both `auto_route_clean` values with private context | 2/2 tests passed | PASS |
| Audit entry count equals outbound connection count | Real sockets: the stand-in frontier server counts TCP connections; blocked requests add none | 1/1 tests passed | PASS |
| Killing the local model returns an error, never a frontier response | Stop the stand-in model; check every path including streaming | 3/3 tests passed | PASS |
| Planted prompt-injection text claiming "not sensitive" does not alter taint | Hostile context; client-supplied taint fields and reserved sources | 4/4 tests passed | PASS |
| `/new` is the only way taint is cleared | Topic-change wording, case variants and partial matches must not clear it | 2/2 tests passed | PASS |
| Frontier receives only the current user message; attachments rejected | Inspect the recorded frontier request bodies | 2/2 tests passed | PASS |
| Only the egress function can reach the frontier client | Static check of imports | 1/1 tests passed | PASS |
| Audit log holds no prompt bodies or user data | Search the log file for the prompt text; compare the hash | 1/1 tests passed | PASS |
| Frontier failure is an error, is still audited, and does not fall back | Stop the stand-in frontier; remove the API key | 2/2 tests passed | PASS |
| Users are isolated; adding a user is a config change | Second user added through config only | 1/1 tests passed | PASS |
| Explicit private lane and lane labelling | Lane and taint fields on every response | 3/3 tests passed | PASS |
| **Phase 3: private documents** | | | |
| Golden set: recall@5 and MRR (offline stand-in embedder) | 50 questions over an 11-document corpus including a PDF | 3/3 tests passed; recall@5 0.920, MRR 0.840 | PASS |
| Off-topic no-answer questions get "not in documents" from the floor gate without calling a model | 10 no-answer questions; the 6 off-topic ones must be gated; the 4 near-miss ones are covered separately below | 2/2 tests passed; floor gate closed on 6/10 of all 10 (see note 2); false abstentions 5/50 | PASS |
| User A can never retrieve user B's chunks | Two users, hostile filters, whole golden corpus, and through the gateway | 3/3 tests passed | PASS |
| Chunks inherit source taint; documents use the private lane only | Registered and unregistered sources; auto routing and consent on | 4/4 tests passed | PASS |
| Ingest md, txt and pdf; chunking; storage backend guard | Sample files; broken and unsupported files | 6/6 tests passed | PASS |
| Retrieval fails closed; documents can be disabled | Embedder down; feature switched off | 3/3 tests passed | PASS |
| Instructions inside a document are data, not commands | Document with a fake closing tag and instructions | 2/2 tests passed | PASS |
| Evaluation harness computes faithfulness and correctness | Stand-in answerer and judge (plumbing only) | 1/1 tests passed | PASS |
| **Phase 4: agent and permission layer** | | | |
| Every write, exec and web tool call is blocked at the gateway | A misbehaving stand-in model issues 14 kinds of forbidden call; the execution endpoint is probed too | 26/26 tests passed | PASS |
| Tool definitions come from the gateway; the model never sees the client's | Client sends hostile definitions | 4/4 tests passed | PASS |
| Tool arguments validated; call count capped; ids issued by the gateway | Malformed, oversized and extra arguments | 10/10 tests passed | PASS |
| Path sandbox: no `..`, absolute paths, symlink escapes or other users' files | Eight escape attempts, also through the tool bridge | 11/11 tests passed | PASS |
| Tool results are Fragments with taint; tools cannot launder private data into CLEAN | Default PRIVATE, argument taint, client-supplied labels | 4/4 tests passed | PASS |
| Tool results are accepted only for calls the gateway issued | Forged, replayed, blocked and cross-user results | 5/5 tests passed | PASS |
| Tool output cannot break its wrapper or run `/new` | Fake closing tag and slash command inside tool output | 2/2 tests passed | PASS |
| Injection corpus: 12 hostile documents x 4 channels cannot change lane, taint or exfiltrate | A stand-in model that obeys every injection; assert lane, taint, zero frontier connections, blocked tools, sanitised reply | 48/48 tests passed | PASS |
| Injection corpus, streamed replies | Same documents, replies streamed in 3-character pieces | 14/14 tests passed | PASS |
| Streaming cannot bypass the tool check | Streamed reply with forbidden calls | 2/2 tests passed | PASS |
| 20-task tool-use scorer works (stand-in models only) | Correct, wrong-tool, never-stops and write-happy stand-ins; tasks solvable from real tool output | 6/6 tests passed; stand-in scored 20/20 (proves the harness only) | PASS |
| Agent tool bridge and agent configuration | Bridge run against a live gateway over stdio; config checks | 11/11 tests passed | PASS |
| **Built-in test UI (`/ui`)** | | | |
| UI is served with a strict content policy and only its own two assets | Header checks, path-traversal probes, config switch | 3/3 tests passed | PASS |
| UI makes no outside requests and never renders server text as HTML | Static scan of the page, script and stylesheet; script parses; every element id it uses exists | 4/4 tests passed | PASS |
| Audit and security-log endpoints return only the caller's own records, with no prompt text | Two users; limit and ordering; auth required | 4/4 tests passed | PASS |
| UI works in a real browser: login, chat, consent (both scopes), taint lock, /new, tools, logs, mobile layout | Headless Chrome drives the real page against the real gateway with stand-in models; also fails on any content-policy violation | 2/2 tests passed | PASS |
| **Phase 5: packaging and hygiene** | | | |
| Secrets are never tracked; env example holds no values | Scan tracked files | 3/3 tests passed | PASS |
| README and docs carry no personal references | Scan for emails, home paths and account URLs; optional name scrub | 3/3 tests passed | PASS |
| Nothing is published publicly by default | Read the Compose file: loopback bind, no model port, internal network, locked-down container | 4/4 tests passed | PASS |
| README states the routing table and rules verbatim | Exact-text check | 2/2 tests passed | PASS |
| Other tests (parametrised cases and helpers not listed above) | Same suites | 3 passed, 1 skipped (optional name scrub) | PASS |

## Not run: needs a real GPU host

| Check | Method | Where | Status |
|---|---|---|---|
| All 10 no-answer questions, including the 4 near-miss ones, return "not in documents" | Real model following the grounding instruction (`tests/rag/report.py --live`) | RUNBOOK 6.6 | PARTIAL: 6/10 verified offline; requirement not yet met |
| Outside port scan shows nothing open | `tests/test_portscan.sh` from another machine | RUNBOOK 6.2 | NOT RUN |
| Audit entries equal real outbound connections on the wire | `tests/privacy/tcpdump_check.sh` on the host | RUNBOOK 6.3 | NOT RUN |
| Host firewall limits the gateway to the frontier API host | `infra/egress-allowlist.sh`; verified by the capture above | RUNBOOK 5 | NOT RUN |
| A real local model answers through the gateway | `curl` through the gateway on the GPU host | RUNBOOK 6.1 | NOT RUN |
| Killing the real Ollama container returns an error, not a frontier response | Stop the container and send a request | RUNBOOK 6.5 | NOT RUN |
| Retrieval quality with the real embedding model, and `rag.min_score` calibration | `tests/rag/report.py` floor sweep | RUNBOOK 6.6 | NOT RUN |
| Faithfulness and answer correctness on the golden set | `tests/rag/report.py --live` | RUNBOOK 6.6 | NOT RUN |
| Tool-use score of the real daily-driver model on the 20-task set | `tests/agent/run_eval.py --url ... --model ...` | RUNBOOK 6.6 | NOT RUN |
| A live frontier call, including the audit record and token counts | Real API key on the host | RUNBOOK 4 | NOT RUN |
| OpenClaw runs with `agent/openclaw.json` and the tool bridge | Install OpenClaw, validate config, run a task | agent/README.md | NOT RUN |

## Notes

1. **Retrieval numbers come from an offline stand-in embedder.** It matches words, not meaning. Recall@5
   0.920 and MRR 0.840 say nothing about the real embedding model, which should do better on paraphrases.
2. **The "not in documents" gate is only a first filter.** It closed on 6/10 no-answer questions at the shipped
   floor. The six off-topic questions are all caught. The near-miss questions (topic present, fact absent)
   are not separable by a score floor with this embedder: raising it enough to catch them rejects most answerable
   questions. Those cases rely on the local model following its instruction to reply `not in documents`, which is
   **unverified** until a real model is run. False abstentions at the shipped floor: 5/50.
3. **Faithfulness is not measured.** It needs a real model. The harness and the live mode exist and their plumbing is tested.
4. **The tool-use score is not measured for any real model.** The scorer works and the 20 tasks are solvable from real
   tool output, but the only score so far is a scripted stand-in. Raw self-check output:

```
tasks passed        : 20/20
correct tool        : 1.000
correct arguments   : 1.000
correct stop/answer : 1.000
calls blocked by the gateway permission check: 0
```

5. **OpenClaw has not been run.** Its configuration was written from documentation and is unverified.
